server:
  port: 8888

spring:
  application:
    name: config-server
  cloud:
    config:
      server:
        git:
          uri: https://github.com/hoanglinh807607/config-server-dissertation.git
          default-label: main
          clone-on-start: true